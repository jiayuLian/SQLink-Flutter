# SQLink（Flutter 版）基础功能

手机上的 MySQL 客户端，用 Flutter 重写，Android / iOS 双端通用，全功能免费、无会员体系。

## 基础功能
- **多连接管理**：保存多个数据库连接（主机 / 端口 / 用户 / 默认库 / 密码 / TLS / 自签信任）
- **密码安全存储**：iOS Keychain、Android 加密存储，明文不落盘
- **底部双标签**：数据库 / 我的（我的 = 外观、查询设置、关于）
- **库表浏览**：数据库 → 表 / 视图 → 表详情（列结构 + key 徽标 + 备注）→ 查看数据；列表顶部支持实时搜索
- **表详情页**：列结构、查看建表 SQL（可缩放 / 复制）、匹配行数、进入查询控制台
- **表数据浏览**：分页（可设每页条数 + 缩放显示）、排序、WHERE 筛选、行数统计、CSV / SQL 导出
- **表数据内联编辑**：设置筛选或排序后可就地改单元格，避免误改全表
- **查询控制台**：任意 SQL、多语句、SQL 历史、关键字 / 表名 / 字段名 / 别名自动补全、CSV / SQL 导出
- **筛选&排序**：19 个运算符（含 不开始于 / 不结束于 / 在列表 / 不在列表）+ 多条件 AND / OR + 关联排序
- **连接错误分类提示**：账号错 / 密码错 / 网络不可达分别给出中文提示
- **TLS 加密连接**：支持自签名证书信任
- **认证兼容**：mysql_native_password 与 caching_sha2_password（MySQL 8 默认）
- **iOS 风格交互**：连接列表左滑为窄条按钮、行内铅笔编辑、长按菜单、删除确认弹窗
- **iOS 观感**：系统蓝强调色、分组卡片版式、等宽字体网格、居中导航标题（对齐 Swift 版视觉）

## 安装
- **Android**：下载 `SQLink-Flutter.apk`，允许「未知来源」后安装
- **iOS**：用 **TrollStore** 打开 `SQLink-Flutter.ipa` 安装（未签名 IPA，安装时自动签名，无需 Apple ID）

## 更新记录

> 仅记录真正的代码改动（新增功能 / 修复 bug）。文档与构建流程调整不在此列。

- **2026-09-11**
  - 🐞 修复：编辑模式下仍可切换「每页条数」，换页后旧的编辑值会写到新页的行上（数据被改坏）；现编辑期间锁定翻页 / 筛选 / 每页条数，并对意外触发的数据加载做「先退出编辑」兜底
  - 🐞 修复：SQL 中字符串或注释里的分号会被当成语句分隔符，`WHERE a='x;y'` 被误切成两条语句而报语法错误；现改为引号 / 注释感知的语句切分
  - 🐞 修复：保存编辑时未校验影响行数，主键为 NULL 的行会拼出 `WHERE pk = ''`，匹配不到任何记录却提示「保存成功」；现跳过这类行并提示「N 行未匹配到记录，未生效」
  - 🐞 修复：结果集不含主键列（列级 SELECT 权限 / 隐藏列等）时仍可进入编辑，保存时才失败；现直接禁用编辑入口并说明原因
  - ⚙️ 对齐：库 / 表列表的「新建查询」首行改为表列表导航栏「查询」入口（对齐 Swift）
  - 🐞 修复：查询控制台内联编辑「保存成功」后，只读结果表格仍显示旧值（保存时只更新了内部副本，未刷新结果集）
  - 🐞 修复：空表（有列名但 0 行）在结果表格里被误显示为「无结果集」，现改为显示列头
  - 🐞 修复：表数据页「导出 CSV / SQL」此前只导出当前页，现按当前筛选 / 排序导出**全部匹配行**（分块拉取），文件名带时间戳
  - 🐞 修复：连接列表解析改为逐条容错，避免一条损坏记录导致整个连接列表被清空
  - 🐞 修复：建连过程中返回上一页会残留未关闭的连接（socket 泄漏）
  - ⚙️ 对齐：连接编辑页补「取消」按钮；端口校验加上界（1-65535）
  - ⚙️ 对齐：查询历史保留上限 30 条（原 50），页面标题改为「查询历史」
  - ⚙️ 对齐：编辑态表格主键列底色由琥珀色改为主题色 10%，表头改标 🔑🔒（🔒 表示该列只读锁定）
  - ⚙️ 对齐：连接行副标题的默认库写成 `/db`（去掉空格）；空列表提示文字置灰；「新建查询」行补下级箭头
  - 🎨 样式对齐：整体视觉对齐 Swift 版
    - 强调色改为 iOS 系统蓝（对齐 SwiftUI `.accentColor`）；页面底色改为 iOS 分组列表底（浅色 #F2F2F7 / 深色纯黑），卡片浮于其上；导航标题居中
    - 底部标签改为平铺风格标签栏（图标 + 文字、选中主题色），去掉 Material 3 的圆角指示药丸
    - 结果表格配色对齐：表头灰 18%（主键列主题色 18%）、隔行灰 4%、主键列主题色 10%
    - 点单元格改为「列名标题 + 等宽正文 + 复制完整值」弹窗；新增长按单元格直接复制
    - 查询控制台 SQL 输入框改为圆角 8 + 灰色细描边（对齐 Swift TextEditor）；补全候选改为等宽字体 + 主题色 12% 底
    - 建表 SQL 弹窗正文加次级背景色圆角卡片
    - 表详情页 /「我的」页改为 iOS 分组卡片版式
  - 🐞 修复：表数据页首次进入可能按默认 100 条加载，而非「我的 → 默认每页条数」设置值（并消除两次并发加载互相覆盖）
  - 🐞 修复：筛选弹窗中排序字段 / 字段名不属于当前表时下拉框断言崩溃（对齐 Swift 的排序字段归零保护）
  - 🐞 修复：连接失败或中途返回时 loading 遮罩可能残留、吞掉页面点击
  - 🐞 修复：退出查询控制台时未保存当前 SQL（对齐 Swift `onDisappear`），在表内入口退出后再进来可恢复上次输入的 SQL
  - 🐞 修复：控制台结果表格与外层列表嵌套纵向滚动导致内层滚动失效，现固定 320 高（对齐 Swift）
  - ⚙️ 对齐：整体操作习惯与 Swift 版 SQLink 逐屏对齐
    - 主界面改为底部双标签「数据库 / 我的」（我的 = 外观 / 查询设置 / 关于）
    - 连接列表：新增入口移到导航栏右上角「+」，行内铅笔可直接编辑，长按弹出「编辑 / 删除」
    - 库 / 表列表：表列表导航栏提供「切换库」与「查询」入口（查询入口最终形态见上方对齐说明）
    - 进入表改为两级：先到「表详情」（列结构 + key 徽标 + 备注、查看建表 SQL、匹配 N 条），再进「查看数据」
    - 表数据页：导航栏左侧「筛选&排序」、右侧「编辑 / 导出」；底部分页栏「上一页 / 页码 / 下一页 + N 条/页 + 缩放%」
    - 筛选&排序弹窗改为居中卡片（点击遮罩关闭）；结果表格去掉行号列
  - ✨ 新增：筛选运算符补齐「不开始于 / 不结束于 / 在列表 / 不在列表」
  - ✨ 新增：建表 SQL 弹窗支持双指缩放、双击或「1:1」复位、一键复制
  - 🐞 修复：从表列表页打开查询控制台时会带出上次在某张表下输入的 SQL（曾出现默认第一张表的 SQL），现改为进入即空白控制台，SQL 由用户自行输入
  - ✨ 新增：查询控制台 SQL 自动补全增强
    - 输入框开头打字（如 `SEL`）即提示，不再空白
    - 关键词支持多词短语：输入 `OR` 提示 `ORDER BY`，`SEL` 提示 `SELECT` / `SELECT *`
    - 表名前缀 / 模糊匹配库中表（如 `lvv_c` 匹配 `lvv_user_config`、`lvv_exchange_record`）
    - 表名出现在 SQL 中即加载其字段，便于表名后直接补全字段
  - ✨ 新增：表别名 / 表名前缀字段补全（`alias.` / `table.` / `db.table.`）
    - 解析 `FROM t a`、`JOIN t b`、`FROM t1 a, t2 b` 等别名写法
    - 输入 `a.` / `b.` 即补全各自表的字段，服务于关联查询
  - 🐞 修复：点击补全时误删别名前缀（`a.` 被删成 `id`，应为 `a.id`）
  - 🐞 修复：查询控制台在编辑态下直接重新执行 SQL 时，单元格输入控制器未被释放（反复「运行 → 编辑」会在内存中累积）；现先按「取消编辑」收尾再执行
  - ⚡ 优化：查询控制台自动补全候选每次重建只计算一次（原会跑两遍别名正则与表名 / 字段匹配），输入更跟手

- **2026-09-09**
  - ✨ 新增：数据库列表 / 表列表顶部实时搜索（对齐 Swift 版 `.searchable`）
  - 🐞 修复：新建 / 编辑连接中密码「眼睛」图标方向反了（睁眼时显示明文、闭眼时显示密文）
  - 📄 文档：README 更新为技术说明（包名、产物名、错误分类、NAT64 兼容、权限、滑动样式等）
