name: Analyze (develop only)

on:
  push:
    branches: [develop]

permissions:
  contents: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello from develop analyze probe"
      - run: uname -a
